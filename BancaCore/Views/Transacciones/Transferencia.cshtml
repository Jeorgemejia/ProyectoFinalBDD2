@{ ViewData["Title"] = "Transferencia"; }

<div class="d-flex justify-content-between align-items-center mb-4">
    <h5 class="page-title mb-0"><i class="bi bi-arrow-left-right me-2 text-primary"></i>Transferencia Entre Cuentas</h5>
    <a asp-action="Index" class="btn btn-sm btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Historial</a>
</div>

<div class="card" style="max-width:520px">
    <div class="card-body p-4">
        <form asp-action="Transferencia" method="post">
            @Html.AntiForgeryToken()
            <div class="mb-3">
                <label class="form-label fw-semibold">Cuenta Origen</label>
                <select name="cuentaOrigen" asp-items="ViewBag.Cuentas" class="form-select">
                    <option value="">-- Cuenta origen --</option>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Cuenta Destino</label>
                <select name="cuentaDestino" asp-items="ViewBag.Cuentas" class="form-select">
                    <option value="">-- Cuenta destino --</option>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Moneda</label>
                <select name="moneda" asp-items="ViewBag.Monedas" class="form-select">
                    <option value="">-- Moneda --</option>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Monto</label>
                <div class="input-group">
                    <span class="input-group-text">Q</span>
                    <input type="number" name="monto" class="form-control" min="0.01" step="0.01" placeholder="0.00" required/>
                </div>
            </div>
            <div class="mb-4">
                <label class="form-label fw-semibold">Descripción</label>
                <input type="text" name="descripcion" class="form-control" placeholder="Concepto de transferencia"/>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-banca fw-semibold"><i class="bi bi-check-lg me-1"></i>Transferir</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>
