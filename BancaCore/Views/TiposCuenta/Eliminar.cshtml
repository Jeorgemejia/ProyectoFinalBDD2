@model BancaCore.Models.TipoCuenta
@{ ViewData["Title"] = "Eliminar Tipo de Cuenta"; }

<div class="card" style="max-width:700px">
 <div class="card-body p-4">
  <p>¿Eliminar tipo de cuenta <strong>@Model.Nombre</strong>?</p>
  <form asp-action="Eliminar" method="post">@Html.AntiForgeryToken()<input type="hidden" name="id" value="@Model.CodigoTipoCuenta" />
  <div class="mt-3"><button class="btn btn-danger">Eliminar</button> <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a></div>
  </form>
 </div>
</div>
